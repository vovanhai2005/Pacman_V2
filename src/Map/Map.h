#pragma once

#ifndef _MAP_H
#define _MAP_H

#include <SDL.h>
#include <stdio.h>

class Map{
    private:
        static const int MAP_HEIGHT = 28;
        static const int MAP_WIDTH = 31;
        
    public:
        Map();

        ~Map();

        void    
}
#endif 